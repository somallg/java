<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <context:property-placeholder location="classpath:jax-ws-servlet-tomcat-config.properties"/>

<!--    <bean class="java.net.URL" id="url">
        <constructor-arg value="${wsdlDocumentUrl}"/>
    </bean>

    <bean class="javax.xml.namespace.QName" id="qName">
        <constructor-arg value="${namespaceUri}"/>
        <constructor-arg value="${serviceName}"/>
    </bean>

    <bean class="javax.xml.ws.Service" factory-method="create" id="service">
        <constructor-arg ref="url"/>
        <constructor-arg ref="qName"/>
    </bean>

    <bean class="com.somallg.jax.ws.servlet.tomcat.HelloWorld" factory-bean="service" factory-method="getPort"
          id="port">
        <constructor-arg value="${serviceInterface}"/>
        <constructor-arg>
            <bean class="javax.xml.ws.soap.AddressingFeature">
                <constructor-arg value="false"/>
            </bean>
        </constructor-arg>
    </bean>-->

    <!-- Create WS Client using Spring -->
    <bean class="org.springframework.remoting.jaxws.JaxWsPortProxyFactoryBean">
        <property name="serviceInterface" value="${serviceInterface}"/>
        <property name="wsdlDocumentUrl" value="${wsdlDocumentUrl}"/>
        <property name="namespaceUri" value="${namespaceUri}"/>
        <property name="serviceName" value="${serviceName}"/>
        <property name="portName" value="${portName}"/>
    </bean>

    <!--<bean class="com.somallg.logger.injector.LoggerInjector"/>-->
</beans>